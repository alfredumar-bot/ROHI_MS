from kivymd.uix.screen import MDScreen


class SettingsScreen(MDScreen):
    """
    Settings screen: GPS/Geofencing radius, Reset Database (with confirmation),
    and the entry point into Server Connection.
    All logic lives in main.py's ROHIAttendanceApp, mirroring the rest of the app.
    """
    pass
