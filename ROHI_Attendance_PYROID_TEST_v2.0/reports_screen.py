from kivymd.uix.screen import MDScreen


class ReportsScreen(MDScreen):
    """
    Attendance Reports screen: month/year/day period selection, an on-screen
    summary + table for the selected period, and PDF/Excel export (both for
    the selected period and for the full attendance history to date).
    All logic lives in main.py's ROHIAttendanceApp, mirroring the rest of the app.
    """
    pass
